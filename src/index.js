import React from 'react';
// new method to import ReactDOM
import ReactDOM from 'react-dom/client';
import App from './app';

// new method to render ReactDOM
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);